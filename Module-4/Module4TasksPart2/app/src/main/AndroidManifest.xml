<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Разрешение для показа уведомлений (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Разрешение для запуска foreground-сервиса -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Тип foreground-сервиса: специальная короткая задача -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Module4TasksPart2">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Module4TasksPart2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Задание 5: Foreground-сервис счётчика времени -->
        <service
            android:name=".CounterForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="counter" />
        </service>

        <!-- Задание 6: Background-сервис таймера -->
        <service
            android:name=".TimerBackgroundService"
            android:exported="false" />

        <!-- Задание 7: Bound-сервис случайных чисел -->
        <service
            android:name=".RandomNumberService"
            android:exported="false" />

    </application>

</manifest>